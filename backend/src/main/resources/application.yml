spring:
  application:
    name: rail12306

  datasource:
    url: jdbc:mysql://localhost:3306/rail12306
    username: root
    password: rail12306

  flyway:
    enabled: true

mybatis:
  type-handlers-package: cn.nispring.rail12306.typehandler
  configuration:
    map-underscore-to-camel-case: true

data:
  dir: ../data
  imported: false
