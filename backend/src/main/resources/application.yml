spring:
  application:
    name: rail12306

  datasource:
    url: jdbc:mysql://localhost:3306/rail12306
    username: root
    password: rail12306

  flyway:
    enabled: true

  mvc:
    # 预期内、已被 GlobalExceptionHandler 正常处理的异常，不再由框架打 WARN
    log-resolved-exception: false

mybatis:
  type-handlers-package: cn.nispring.rail12306.typehandler
  configuration:
    map-underscore-to-camel-case: true

data:
  dir: ../data
